# Build artefacts: the PDF is produced from the tracked sources, never committed.
*.pdf
*.xdv
*.aux
*.log
*.out
*.toc

# Japanese fonts are fetched by the reader; see README.md. The pattern has no trailing
# slash so that it also ignores a `fonts` symlink pointing at a local font directory,
# which `fonts/` would not match.
fonts
